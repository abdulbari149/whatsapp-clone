import React from 'react'
import { User } from '../interface'

interface Props{
    user: User
}

const Contact:React.FC<Props> = ({  }) => {
    return (
        <div>
            
        </div>
    )
}

export default Contact
